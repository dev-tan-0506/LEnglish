---
phase: 01-authentication-system
plan: 05
type: execute
wave: 4
depends_on: ["03", "04"]
files_modified:
  - apps/web/src/lib/api/profile.ts
  - apps/web/src/lib/profile/options.ts
  - apps/web/src/components/profile/OnboardingScoreSelector.tsx
  - apps/web/src/components/profile/AvatarPicker.tsx
  - apps/web/src/components/profile/EnglishLevelSelector.tsx
  - apps/web/src/components/profile/ProfileForm.tsx
  - apps/web/src/app/(app)/onboarding/page.tsx
  - apps/web/src/app/(app)/profile/page.tsx
  - apps/web/src/components/profile/profile.test.tsx
autonomous: true
requirements: [AUTH-03]
must_haves:
  truths:
    - "Newly registered user can choose a target TOEIC score in one onboarding step."
    - "User can view and edit profile fields required by D-10."
    - "User can choose preset avatar or upload a custom avatar."
  artifacts:
    - "apps/web/src/components/profile/OnboardingScoreSelector.tsx"
    - "apps/web/src/components/profile/ProfileForm.tsx"
    - "apps/web/src/components/profile/AvatarPicker.tsx"
    - "apps/web/src/lib/api/profile.ts"
  key_links:
    - "OnboardingScoreSelector calls profile API and redirects to profile/app area after save."
    - "ProfileForm and AvatarPicker use backend profile/avatar endpoints from Plan 03."
    - "Profile options match backend DTO enum values."
---

<objective>
Build onboarding and profile management UI.

Purpose: This completes AUTH-03 from the user's perspective and implements D-07 through D-12 plus the UI-SPEC profile/onboarding contract.
Output: profile API client, target score onboarding, avatar picker, English level selector, profile page, and component tests.
</objective>

<execution_context>
@C:/code/LEnglish/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/LEnglish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-authentication-system/01-CONTEXT.md
@.planning/phases/01-authentication-system/01-UI-SPEC.md
@.planning/phases/01-authentication-system/01-RESEARCH.md
@.planning/phases/01-authentication-system/01-VALIDATION.md
@.planning/phases/01-authentication-system/01-03-SUMMARY.md
@.planning/phases/01-authentication-system/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add profile API client and shared option constants</name>
  <files>apps/web/src/lib/api/profile.ts, apps/web/src/lib/profile/options.ts, apps/web/src/components/profile/profile.test.tsx</files>
  <action>Create profile client functions `getMyProfile`, `updateMyProfile`, and `uploadAvatar`, all using the credentialed fetch wrapper from Plan 04. Create constants for target TOEIC scores and D-12 English levels that exactly match backend DTO accepted values. Tests must fail if UI options drift from expected API enum values.</action>
  <verify>
    <automated>pnpm --filter @lenglish/web test -- profile</automated>
  </verify>
  <done>Profile client exists and option constants match backend contract values.</done>
</task>

<task type="auto">
  <name>Task 2: Build one-step TOEIC score onboarding</name>
  <files>apps/web/src/components/profile/OnboardingScoreSelector.tsx, apps/web/src/app/(app)/onboarding/page.tsx, apps/web/src/components/profile/profile.test.tsx</files>
  <action>Implement D-07 and D-11: one-screen target TOEIC score selector using large visual cards with icons and short descriptions. Scores must include 450, 600, 750, and 900+. Selected state uses the UI-SPEC accent border/glow. Saving calls `updateMyProfile({ targetToeicScore })` and redirects to profile/app area. Add loading, error, disabled, and keyboard focus states.</action>
  <verify>
    <automated>pnpm --filter @lenglish/web test -- profile</automated>
  </verify>
  <done>Onboarding tests cover render, card selection, save call, validation, and redirect behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Build profile editor with avatar and level controls</name>
  <files>apps/web/src/components/profile/AvatarPicker.tsx, apps/web/src/components/profile/EnglishLevelSelector.tsx, apps/web/src/components/profile/ProfileForm.tsx, apps/web/src/app/(app)/profile/page.tsx, apps/web/src/components/profile/profile.test.tsx</files>
  <action>Implement D-09, D-10, and D-12. Profile page loads current profile and edits name, avatar, target TOEIC score, bio, birthdate, and current English level. AvatarPicker supports preset avatars and custom upload. EnglishLevelSelector uses five visual radio cards: Newbie, Beginner, Intermediate, Upper-Intermediate, Advanced. Save button uses UI-SPEC press-down styling and shows success/error toasts.</action>
  <verify>
    <automated>pnpm --filter @lenglish/web test -- profile</automated>
  </verify>
  <done>Profile tests prove all D-10 fields render, edits call profile API, avatar upload calls upload endpoint, and invalid values are blocked before submit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser profile form to API | User-controlled profile values and files are sent to API. |
| Browser route to app area | Authenticated-only UI routes rely on backend session status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-16 | Tampering | Profile form | mitigate | Client validates allowed enum values; backend remains authoritative. |
| T-1-17 | Information Disclosure | Profile page | mitigate | Only fetch `/profile/me`; do not accept arbitrary user IDs in UI. |
| T-1-18 | Denial of Service | Avatar upload | mitigate | Client checks size before upload; backend validates again. |
</threat_model>

<verification>
Run:
```bash
pnpm --filter @lenglish/web test -- profile
pnpm --filter @lenglish/web lint
pnpm test
```
</verification>

<success_criteria>
- AUTH-03 is complete in the frontend.
- D-07 through D-12 are implemented.
- Onboarding is exactly one target-score step after signup/login when needed.
- Profile page supports all required profile fields and avatar paths.
- UI matches the approved UI-SPEC visual contract.
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-system/01-05-SUMMARY.md`.
</output>
