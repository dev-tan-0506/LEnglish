---
phase: 01-authentication-system
plan: 06
type: execute
wave: 5
depends_on: ["02", "03", "04", "05"]
files_modified:
  - apps/api/test/auth-flow.e2e-spec.ts
  - apps/web/tests/e2e/auth-session.spec.ts
  - apps/web/tests/e2e/profile.spec.ts
  - apps/web/tests/e2e/reset-password.spec.ts
  - apps/web/tests/e2e/helpers/auth.ts
  - apps/web/tests/e2e/helpers/mailbox.ts
  - docs/auth-local-runbook.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
must_haves:
  truths:
    - "Full browser flow covers signup, onboarding, profile edit, refresh persistence, logout, login, and password reset."
    - "API e2e tests cover auth/profile/reset contracts without relying only on component tests."
    - "Local runbook documents how to start services and configure SMTP for reset email."
  artifacts:
    - "apps/api/test/auth-flow.e2e-spec.ts"
    - "apps/web/tests/e2e/auth-session.spec.ts"
    - "apps/web/tests/e2e/profile.spec.ts"
    - "apps/web/tests/e2e/reset-password.spec.ts"
    - "docs/auth-local-runbook.md"
  key_links:
    - "Playwright tests drive Next.js UI against the NestJS API."
    - "Reset password e2e uses MailService test mailbox or documented SMTP test transport."
    - "Runbook maps env vars from `.env.example` to local commands."
---

<objective>
Add final integration tests and local runbook for Phase 1.

Purpose: This plan proves that the phase works end-to-end, not just as isolated backend and frontend pieces. It is the execution gate before `$gsd-verify-work`.
Output: API e2e tests, Playwright flows, test helpers, and local auth runbook.
</objective>

<execution_context>
@C:/code/LEnglish/.codex/get-shit-done/workflows/execute-plan.md
@C:/code/LEnglish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-authentication-system/01-CONTEXT.md
@.planning/phases/01-authentication-system/01-UI-SPEC.md
@.planning/phases/01-authentication-system/01-RESEARCH.md
@.planning/phases/01-authentication-system/01-VALIDATION.md
@.planning/phases/01-authentication-system/01-02-SUMMARY.md
@.planning/phases/01-authentication-system/01-03-SUMMARY.md
@.planning/phases/01-authentication-system/01-04-SUMMARY.md
@.planning/phases/01-authentication-system/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API contract e2e coverage</name>
  <files>apps/api/test/auth-flow.e2e-spec.ts</files>
  <action>Create API e2e tests that run against a test database and cover: register sets cookies, duplicate register fails, login sets cookies, `/auth/me` works, refresh rotates refresh token, logout revokes token, profile update persists, reset request emits a mail payload, reset confirm changes password, and old password fails. Use deterministic test data and clean up records between tests.</action>
  <verify>
    <automated>pnpm --filter @lenglish/api test -- auth-flow.e2e</automated>
  </verify>
  <done>API e2e suite proves AUTH-01 through AUTH-04 backend contracts in one flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add browser e2e auth and profile flows</name>
  <files>apps/web/tests/e2e/auth-session.spec.ts, apps/web/tests/e2e/profile.spec.ts, apps/web/tests/e2e/helpers/auth.ts</files>
  <action>Create Playwright tests that start from the login/signup UI and cover: signup, onboarding target score selection, profile edit, browser refresh keeps session active per roadmap success criterion, logout redirects to login, login restores access, and protected profile route redirects when unauthenticated. Tests must verify visible UI states, not just network calls.</action>
  <verify>
    <automated>pnpm --filter @lenglish/web e2e -- auth-session profile</automated>
  </verify>
  <done>Browser tests prove session persistence across refresh and profile management from the user perspective.</done>
</task>

<task type="auto">
  <name>Task 3: Add reset password e2e and local runbook</name>
  <files>apps/web/tests/e2e/reset-password.spec.ts, apps/web/tests/e2e/helpers/mailbox.ts, docs/auth-local-runbook.md</files>
  <action>Create Playwright reset password flow using a test mailbox helper exposed by the API test mail transport. The test requests reset, reads the generated reset link, opens it, sets a new password, and logs in with the new password. Add `docs/auth-local-runbook.md` with commands for `docker compose up -d`, `pnpm install`, migrations, dev servers, SMTP env setup, and verification commands.</action>
  <verify>
    <automated>pnpm --filter @lenglish/web e2e -- reset-password</automated>
  </verify>
  <done>Reset password browser flow is tested and local setup/run instructions are documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner to local services | E2E tests drive both apps and test database. |
| Test mailbox to reset flow | Reset links are exposed only in test environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-19 | Information Disclosure | Test mailbox | mitigate | Gate test mailbox helper behind test environment only. |
| T-1-20 | Tampering | E2E test data | mitigate | Use isolated test database/schema and cleanup hooks. |
| T-1-21 | Repudiation | Runbook | mitigate | Document exact commands and expected pass criteria for local verification. |
</threat_model>

<verification>
Run:
```bash
pnpm test
pnpm e2e
pnpm build
```
</verification>

<success_criteria>
- Every Phase 1 roadmap success criterion has automated e2e coverage.
- AUTH-01 through AUTH-04 pass API and browser tests.
- Session persists across browser refresh.
- Reset password works through email-link flow in test mode.
- Runbook documents local startup and verification.
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-system/01-06-SUMMARY.md`.
</output>
